const db = require('../src/config/db');

// Seed is now done inside migrations/run.js
// Run: npm run migrate

console.log('Seed data is included in migrations/run.js');
console.log('Run: npm run migrate');
process.exit(0);
