{
  "name": "pos-system",
  "version": "1.0.0",
  "description": "POS System — Point of Sale & Inventory Management",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "cd frontend && npm run build",
    "migrate": "cd backend && npm run migrate"
  }
}
